##########
# colors #
##########

# get color from xressource configuration - variable_name xressource_color fallback

# special
# set_from_resource $foreground foreground #C6C6C6
# set_from_resource $background background #1C1C1C
# set_from_resource $cursorColor cursorColor #C6C6C6

# # black
# set_from_resource $black1 color0 #303030
# set_from_resource $black2 color8 #b2b2b2

# # red
# set_from_resource $red1 color1 #d75f5f
# set_from_resource $red2 color9 #d75f5f

# # green
# set_from_resource $green1 color2 #87AF87
# set_from_resource $green2 color10 #87AF87

# # yellow
# set_from_resource $yellow1 color3 #ffd75f
# set_from_resource $yellow2 color11 #ffd75f

# # blue
# set_from_resource $blue1 color4 #87afd7
# set_from_resource $blue2 color12 #87afd7

# # magenta
# set_from_resource $magenta1 color5 #afafd7
# set_from_resource $magenta2 color13 #afafd7

# # cyan
# set_from_resource $cyan1 color6 #afd7ff
# set_from_resource $cyan2 color14 #afd7ff

# # white
# set_from_resource $white1 color7 #eeeeee
# set_from_resource $white2 color15 #ffffff

# Load i3 config files
include ~/.config/i3/config.d/*.conf

###########
# general #
###########

# default_border pixel 1
default_floating_border pixel 1

new_window pixel 1

gaps inner 8
gaps outer 0
# smart_gaps on
# i3 config file (v4)

# Please see http://i3wm.org/docs/userguide.html for a complete reference!
# set the modifier (Mod4: cmd)
# To see a complete list, run xmodmap
set $mod Mod4
set $alt Mod1
set $ctrl Ctrl
set $shift Shift

set $I3_CONFIG "~/.config/i3"

#---Starting External Scripts---# #

# launch brave
# exec --no-startup-id brave

# faster key repeat
exec_always --no-startup-id xset r rate 330 55
bindsym $mod+Ctrl+Shift+k exec --no-startup-id xset r rate 330 55

# Pulse Audio controls
# bindsym --release XF86AudioRaiseVolume exec --no-startup-id ~/.config/i3/scripts/volume.sh +5 #increase sound volume
# bindsym --release XF86AudioLowerVolume exec --no-startup-id ~/.config/i3/scripts/volume.sh -5 #decrease sound volume
# bindsym XF86AudioMute exec --no-startup-id ~/.config/i3/scripts/volume.sh toggle #mute sound
# bindsym XF86AudioStop exec --no-startup-id ~/.config/i3/scripts/volume.sh stop #stop sound

# bindsym --whole-window --border $mod+button4 exec --no-startup-id ~/.config/i3/scripts/volume.sh +5 #increase sound volume
# bindsym --whole-window --border $mod+button5 exec --no-startup-id ~/.config/i3/scripts/volume.sh -5 #decrease sound volume

exec_always --no-startup-id ~/.config/i3/scripts/volumeLimit.sh

bindsym --release XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5%   #increase sound volume
bindsym --release XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle #mute sound

bindsym --whole-window --border $mod+button4 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% #increase sound volume
bindsym --whole-window --border $mod+button5 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% #decrease sound volume

bindsym --release $mod+XF86AudioRaiseVolume exec --no-startup-id playerctl position 5+
bindsym --release $mod+XF86AudioLowerVolume exec --no-startup-id playerctl position 5-


# Hide the bar
# bindsym $mod+h bar mode toggle

# Mouse doesn't jump from one screen to another
mouse_warping none

bindsym $mod+u border none
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:FiraCode Nerd Font 10
# font pango:Inconsolata 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec --no-startup-id kitty
bindsym $mod+Shift+Return exec --no-startup-id kitty --class fterm

# open ranger file manager
# bindsym $mod+p exec --no-startup-id alacritty --class=fterm --title=ranger -e=ranger
# bindsym $mod+p exec --no-startup-id nautilus --browser ~/
bindsym $mod+p exec --no-startup-id thunar
bindsym $mod+t exec --no-startup-id kitty --class=TT --title=TT -e ~/tt.sh

# kill focused window
bindsym $mod+q kill

# update color using pywal
bindsym $mod+Ctrl+w exec --no-startup-id ~/.config/i3/scripts/pywal.sh
bindsym $mod+Ctrl+Shift+w exec --no-startup-id ~/.config/i3/scripts/pywal.sh -r

# start dmenu (a program launcher)
# bindsym $mod+d exec dmenu_run -l 10 -fn Inconsolata -nb '$background' -nf '$foreground' -sb '$red1'
# bindsym $mod+d                  exec --no-startup-id i3-dmenu-desktop --dmenu='dmenu -b -i'
# bindsym $mod+d exec --no-startup-id ~/.config/i3/scripts/rofi.sh
# bindsym $mod+d exec --no-startup-id ~/.config/rofi/launchers/type-3/launcher.sh
# bindsym $mod+d exec --no-startup-id ~/.config/polybar/shapes/scripts/launcher.sh
bindsym $mod+d exec --no-startup-id ~/.config/rofi/launchers/type-3/launcher.sh
# bindsym $mod+BackSpace exec --no-startup-id ~/.config/rofi/powermenu/type-4/powermenu.sh
bindsym $mod+BackSpace exec --no-startup-id ~/.config/rofi/powermenu/type-4/powermenu.sh

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

bindsym $mod+Ctrl+h move workspace to output left
bindsym $mod+Ctrl+l move workspace to output right

bindsym $mod+Ctrl+j workspace next
bindsym $mod+Ctrl+k workspace prev

## Splits
## Mod1 is the Alt key (run xmodmap to see a complete list)

# split in horizontal orientation
bindsym $mod+Mod1+v split v

# split in vertical orientation
bindsym $mod+Mod1+h split h

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# Size of floating windows
floating_minimum_size 150 x 150
# floating_maximum_size 1600 x 1200

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
# bindsym $mod+p focus parent

# focus the child container
# bindsym $mod+c focus child

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 20 # decrease screen brightness

# Touchpad controls
#bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad

# Media player controls
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause && pkill -9 -f cava || true
bindsym XF86AudioPause exec --no-startup-id playerctl play-pause && pkill -9 -f cava || true
# bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous

# mouse control for media
# bindsym --whole-window --border $mod+button1 exec --no-startup-id playerctl previous
# bindsym --whole-window --border $mod+button3 exec --no-startup-id playerctl next

# no focus following the mouse
focus_follows_mouse no

# using the same workspace shortcut will focus on your last 
workspace_auto_back_and_forth no

# workspace layout by default for all workspaces
workspace_layout tabbed

#############
# workspace #
#############

# set $terms "1: terms"
# set $web "2: web"
# set $multimedia "3: multi"
# set $design "4: design"
# set $office "5: office"
# set $knowledge "6: knowledge"
# set $games "7: Games"
set $w1 "1"
set $w2 "2"
set $w3 "3"
set $w4 "4"
set $w5 "5"
set $w6 "6"
set $w7 "7"
set $w8 "8"
set $w9 "9"
set $w10 "10"

# switch to workspace
bindsym $mod+1 workspace $w1
bindsym $mod+2 workspace $w2
bindsym $mod+3 workspace $w3
bindsym $mod+4 workspace $w4
bindsym $mod+5 workspace $w5
bindsym $mod+6 workspace $w6
bindsym $mod+7 workspace $w7
bindsym $mod+8 workspace $w8
bindsym $mod+9 workspace $w9
bindsym $mod+0 workspace $w10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $w1
bindsym $mod+Shift+2 move container to workspace $w2
bindsym $mod+Shift+3 move container to workspace $w3
bindsym $mod+Shift+4 move container to workspace $w4
bindsym $mod+Shift+5 move container to workspace $w5
bindsym $mod+Shift+6 move container to workspace $w6
bindsym $mod+Shift+7 move container to workspace $w7
bindsym $mod+Shift+8 move container to workspace $w8
bindsym $mod+Shift+9 move container to workspace $w9
bindsym $mod+Shift+0 move container to workspace $w10

# move focused workspace between monitors
bindsym $mod+Ctrl+greater move workspace to output right
bindsym $mod+Ctrl+less move workspace to output left

# rename current workspace
bindsym $mod+comma exec --no-startup-id i3-input -F 'rename workspace to "%s"' -P 'New name for this workspace: '
# rename current window
bindsym $mod+period exec --no-startup-id i3-input -F 'exec i3-msg title_format "%s"' -P 'New name for this window: '


# bind software to workspace
# can use xprop - WM_CLASS(STRING) = "urxvt", "URxvt"
# assign [class="Alacritty" instance="Alacritty"] $w1

assign [class="firefox" instance="Navigator"] $w2
# assign [class="Brave-browser" instance="brave-browser"] $w2
assign [class="Chromium" instance="chromium"] $w2
assign [class="Google-chrome" instance="google-chrome"] $w2

assign [class="Slack" instance="slack"] $w3
assign [class="Rhythmbox" instance="rhythmbox"] $w5
assign [class="Bitwarden" instance="bitwarden"] $w3
assign [class="Skype" instance="skype"] $w3

for_window [instance="libreoffice"] move to workspace $w4


for_window [class="Yad"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="file-roller"] floating enable

####################
# restart / reload #
####################

# reload the configuration file
bindsym $mod+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Ctrl+F12 exec i3-msg exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+Shift+r mode "resize"

# Icons
# for_window [all] title_window_icon padding 3px

# URxvt border
for_window [class="URxvt*"] border pixel 1

# floating pop up automatically
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [class="zoom"] floating enable

# floating windows
for_window [class="Arandr"] floating enable, sticky disable, border 1
for_window [class="Gcolor2"] floating enable, sticky disable, border 1
for_window [class="Gnome-control-center"] floating enable, sticky enable
for_window [instance="org.gnome.Nautilus"] floating enable, sticky disable, border 1
for_window [instance="eog"] floating enable, sticky disable, border 1
for_window [class="Blueman-manager"] floating enable, sticky disable, border 1
for_window [class="Thunar"] floating enable, resize set 1920 1080, move position center, sticky disable, border 1
for_window [class="Evince"] floating enable, sticky disable, border 1
for_window [class="gnome-calculator"] floating enable, sticky enable
for_window [class="Gnome-calendar"] floating enable, sticky disable, border 1
for_window [class="Nitrogen"] floating enable, sticky disable, border 1
for_window [class="Lxappearance"] floating enable, sticky disable, border 1
for_window [class="ColorPicker"] floating enable, sticky disable, border 3
for_window [class="Brave-browser"] floating enable, sticky disable, border 3
for_window [class="TT"] floating enable, resize set 740 130, move position center


# pavucontrol
# for_window [class="Pavucontrol"] move to scratchpad, floating enable, sticky enable, resize set 800 600
# bindsym $mod+Ctrl+m [class="Pavucontrol"] scratchpad show
# exec pavucontrol

# Terminal scratchpad with tmux and three windows:
# 1.term 
# 2.vim (on a temp file) 
# 3.translation CLI
# for_window [instance="urxvt_scratchpad"] move to scratchpad
# bindsym $mod+Shift+t [instance="urxvt_scratchpad"] scratchpad show
# exec "$DOTFILES/bash/scripts/scratchpad.sh"

# btop scratchpad
# for_window [instance="btop_scratchpad"] move to scratchpad, sticky enable, border none
# bindsym $mod+Shift+p [instance="btop_scratchpad"] scratchpad show
# exec kitty -name btop_scratchpad -e btop

# keepass scratchpad
# for_window [class="KeePassXC"] move to scratchpad, sticky enable, floating enable, border none
# bindsym $mod+Shift+a [class="KeePassXC"] scratchpad show
# exec keepassxc

# bc scratchpad
# for_window [instance="bc_scratchpad"] move to scratchpad, sticky enable, border none
# bindsym $mod+Shift+b [instance="bc_scratchpad"] scratchpad show
# exec urxvtc -name bc_scratchpad -e bc -l



# start softwares and command lines in workspaces
# exec --no-startup-id i3-msg workspace "$terms"; exec /usr/bin/urxvtc
# exec --no-startup-id i3-msg workspace "$office"; exec joplin-desktop

##########
# DESIGN #
##########

# hide borders
# hide_edge_borders smart

# class                 border  backgr. text    indicator child_border
# client.focused          $red1 $red1 $black1 $background
# client.focused_inactive $green2 $green2 $black1 $green2
# client.unfocused        $red1 $background $black2 $red1
# client.urgent           $red1 $red1 $white2 $red1 $red1
# client.placeholder      $red1 $red1 $white2 $red1 $red1


# set $rosewater #f5e0dc
# set $flamingo #f2cdcd
# set $pink #f5c2e7
# set $mauve #cba6f7
# set $red #f38ba8
# set $maroon #eba0ac
# set $peach #fab387
# set $yellow #f9e2af
# set $green #a6e3a1
# set $teal #94e2d5
# set $sky #89dceb
# set $sapphire #74c7ec
# set $blue #89b4fa
# set $lavender #b4befe
# set $text #cdd6f4
# set $subtext1 #bac2de
# set $subtext0 #a6adc8
# set $overlay2 #9399b2
# set $overlay1 #7f849c
# set $overlay0 #6c7086
# set $surface2 #585b70
# set $surface1 #45475a
# set $surface0 #313244
# set $base #1e1e2e
# set $mantle #181825
# set $crust #11111b

# class                  border     backgr. text    indicator child_border
# client.focused           $lavender $base $green  $rosewater $lavender
# client.focused_inactive  $overlay0 $base $teal  $rosewater $overlay0
# client.unfocused         $overlay0 $base $lavender  $rosewater $overlay0
# client.urgent            $peach    $base $peach $overlay0  $peach
# client.placeholder       $overlay0 $base $text  $overlay0  $overlay0
# client.background        $base

# client.focused          #800080 #800080 #fdf6e3 #800080
# client.focused_inactive #073642 #800080 #fdf6e3 #073642
# client.unfocused        #800080 #073642 #fdf6e3 #800080
# client.urgent           $red1 $red1 $white2 $red1 $red1
# client.placeholder      $red1 $red1 $white2 $red1 $red1

# Start i3bar to display a workspace bar (plus the system information i3status finds out, if available)
# bar {
#     status_command i3status --config ~/.config/i3/i3status.conf
#     position top
#     # Disable all tray icons
#     # tray_output none
#     tray_output primary
#     separator_symbol "┃"
#     colors {
#         background $black1
#         statusline $white2
#         separator $black2
#
#         #                  border background text
#         focused_workspace  $background $background $red1
#         active_workspace   $black1 $black1 $black2
#         inactive_workspace $background $black1 $foreground
#         urgent_workspace   $red1 $red1 $white2
#         binding_mode       $background $red2 $white2
#     }


# lockscreen
# set $i3lockwall i3lock -i $I3_CONFIG/wallpapers/2016-04-07-09.34-Village-in-the-sky.png -t
# set $i3lockwall sh ~/.config/i3/scripts/lock.sh
set $i3lockwall ~/.config/i3/scripts/lock.sh

# In order to cache an image
# exec --no-startup-id betterlockscreen -u $I3_CONFIG/wallpapers/snow_tree.png 

# exec --no-startup-id betterlockscreen -w
# set $i3lockwall exec betterlockscreen --lock
bindsym $mod+Ctrl+Shift+l exec --no-startup-id $i3lockwall
bindsym $alt+l exec --no-startup-id $i3lockwall

#wallpaper and transparency
# exec --no-startup-id compton -b --active-opacity 1 --config $XDG_CONFIG_HOME/.compton --backend xrender --vsync opengl
exec_always --no-startup-id ~/.config/i3/scripts/picom.sh
# exec_always --no-startup-id picom -b
# start slack
# exec --no-startup-id slack

# start google-chrome
exec --no-startup-id google-chrome

# start alacritty
# exec --no-startup-id alacritty

# shutdown / restart / suspend...
set $mode_system \
System (l) lock, (e) logout, (s) suspend, \
(h) hibernate, (r) reboot, (Ctrl+s) shutdown

mode "$mode_system" {
    bindsym l exec --no-startup-id $i3lockwall & sleep 30 && xset dpms force off, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $i3lockwall && systemctl suspend, \
    mode "default"
    bindsym h exec --no-startup-id $i3lockwall && systemctl hibernate, \
    mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Ctrl+s exec --no-startup-id systemctl poweroff -i, \
    mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# bindsym $mod+BackSpace mode "$mode_system"

## Manual management of external displays
set $mode_display Config monitor resolution - Home Setup (h) - Without monitor (d) - Work (w)

mode "$mode_display" {
    # bindsym h exec --no-startup-id sh $XDG_CONFIG_HOME/config_monitors/home.sh && sh $XDG_CONFIG_HOME/.fehbg, mode "default"
    # bindsym d exec --no-startup-id sh $XDG_CONFIG_HOME/config_monitors/default.sh && sh $XDG_CONFIG_HOME/.fehbg, mode "default"
    bindsym w exec --no-startup-id sh ~/.screenlayout/office.sh, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# bindsym $mod+a mode "$mode_display"
bindsym $mod+a focus parent

# Remap caps lock when new keyboard attached
# bindsym $mod+z exec "setxkbmap -option 'caps:ctrl_modifier';xcape -e 'Caps_Lock=Escape' &"

# exec --no-startup-id $I3_CONFIG/scripts/remaps.sh
exec_always --no-startup-id ~/.screenlayout/office.sh
# exec_always --no-startup-id ~/.screenlayout/stop-nvidia.sh

bindsym $mod+Ctrl+f fullscreen toggle global

# Switch keylock on at startup
exec --no-startup-id numlockx on

# wallpaper
exec_always --no-startup-id nitrogen --restore
# colorscheme
exec --no-startup-id wal -R


## Screenshots
bindsym Print exec --no-startup-id flameshot gui

# bindsym Print exec --no-startup-id maim -o "/home/$USER/Pictures/$(date)"
bindsym $mod+Print exec --no-startup-id maim -o --window $(xdotool getactivewindow) "/home/$USER/Pictures/$(date).png"
bindsym Shift+Print exec --no-startup-id maim -o --select "/home/$USER/Pictures/$(date).png"

## Clipboard Screenshots
bindsym Ctrl+Print exec --no-startup-id maim -o | xclip -selection clipboard -t image/png
bindsym Ctrl+$mod+Print exec --no-startup-id maim -o --window $(xdotool getactivewindow) | xclip -selection clipboard -t image/png
bindsym Ctrl+Shift+Print exec --no-startup-id maim -o --select | xclip -selection clipboard -t image/png

# chrome saving password
exec --no-startup-id /usr/bin/gnome-keyring-daemon --start --components=secrets


# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show
bindsym $mod+grave scratchpad show

for_window [class="fterm"] floating enable, resize set 1920 1080, move position center

# autotiling
exec_always --no-startup-id autotiling -w 1 3

# polybar
# exec_always --no-startup-id sleep 2 && ~/.config/polybar/launch.sh --shapes
exec_always --no-startup-id sleep 2 && ~/.config/polybar/launch.sh

bindsym $alt+Tab exec --no-startup-id ~/.config/i3/scripts/i3-alt-tab.py next all
bindsym $alt+Shift+Tab exec --no-startup-id ~/.config/i3/scripts/i3-alt-tab.py prev all

bindsym $alt+grave exec --no-startup-id ~/.config/i3/scripts/i3-alt-tab.py next current
bindsym $alt+Shift+grave exec --no-startup-id ~/.config/i3/scripts/i3-alt-tab.py prev current


# dunst notification
bindsym Ctrl+grave exec --no-startup-id dunstctl history-pop
bindsym Ctrl+Escape exec --no-startup-id dunstctl close

# bluetooth earbuds
bindsym $mod+Ctrl+plus exec --no-startup-id ~/bluetoothC.sh
bindsym $mod+Ctrl+minus exec --no-startup-id ~/bluetoothD.sh

# yad calendar
bindsym $mod+y exec --no-startup-id yad --calendar --show-weeks --center

# calculator
bindsym Ctrl+$mod+C exec --no-startup-id gnome-calculator

# setting

bindsym $mod+s exec --no-startup-id gnome-control-center

# authentication agent
exec --no-startup-id /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1 &


# exec_always --no-startup-id gsettings set org.gnome.settings-daemon.plugins.xsettings overrides "[{'Gdk/WindowScalingFactor', <1.25>}, {'Xft/DPI', <196608>}, {'Xcursor/Size', <25000>}, {'Xcursor/size', <25000>}]"
# exec_always --no-startup-id gsettings set org.gnome.desktop.interface cursor-size 25
# exec_always --no-startup-id xrdb -merge ~/.Xresources
# exec_always --no-startup-id xrandr --dpi 144

# You can also use any non-zero value if you'd like to have a border
for_window [class=".*"] border pixel 0

# make window sticky
bindsym $mod+Ctrl+s sticky toggle
